# Quickstart: Voice Chat Web UI

## Local run

1. Start backend:

```bash
cd backend
uv run --with uvicorn uvicorn app.main:app --reload
```

2. Start frontend:

```bash
cd frontend
npm install
npm run dev
```

3. Open UI:

```
http://localhost:3000
```
